/**
 * Dummy config file for testing
 *
 * @package  Svelte universal component compiler
 * @since:   2020-08-24
 */

module.exports = {
  dev: true,
  customElement: false, // generate Svelte runtime components in most app builds (not WebComponents)
}
